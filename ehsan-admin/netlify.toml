[[edge_functions]]
  path = "/api/send-push"
  function = "send-push"

[[edge_functions]]
  path = "/api/save-subscription"
  function = "save-subscription"

# Redirect root to admin panel
[[redirects]]
  from = "/"
  to = "/index.html"
  status = 200

[build]
  publish = "."
